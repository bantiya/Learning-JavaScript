/** 
 * Basic if Statement
 */


condition = false
if(condition) {
    let abc = "abc"
    console.log(`If Statement ${abc}`);
}
else {
    console.log(`Else Statement ${abc}`);   // abc out of scope
}